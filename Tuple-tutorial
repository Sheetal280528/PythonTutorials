#tuple is also one builtit datatype similar to list but list is mutable (canbe edited) and tuple is immuatble like strings (can not be edited)
list=[]           #empty list
print(list)       #[]
tup=()
print(tup)        #()
print(type(tup))  #<class 'tuple'>

tup1 =(1,)        #single value tuple so always declare like float tuple (7.5,) or str tuple ("hello",)
print(type(tup1)) #<class 'tuple'>
tup2 = (1)        #this is not tuple, its just a int variable which can be written in the fashion (1+2)
print(type(tup2)) #<class 'int'>