# type casting or conversion is basically to convert one datatype to other
# type conversion is implicit by Python but type casting is done by coder
#
a = 2
b = 2.134
sum = (a+b) # implicitely python will convert a into float here like 2.0+2.134 and sum will be 4.134
print (sum) 

a = '23.90'
#b = 2.134
b = '456'
# if both varaible are string then sum = '2'+'2.134' = 22.134 or '234'+'2.134' = 2342.134
# '23.90'+'456' = 23.90456 --> simply + concatinates the strings 
sum = (a+b) # ypeError: can only concatenate str (not "float") to str
print (sum)

a = float ("2") #type casting
b = 4.5
sum = a+b
print (type(a)) # this will print <class 'float'>
print (sum)

a = 5.1
a = str(a)
print (type(a)) #<class 'str'>
print (str(a)) # 5.1
